// Vercel API bundle. Rebuild with npm run build:vercel-api; PDFKit stays lazy-loaded.
import { createRequire as __createRequire } from 'node:module';
const require = __createRequire(import.meta.url);
import{load}from"./chunk-NSTBMTHL.js";import"./chunk-FKC3VD5A.js";import"./chunk-SJYBEHZK.js";var LINK_PROVIDER_ORIGIN="https://majestic.com";var LINK_PROVIDER_PATH="/reports/majestic-million";var RANK_PROVIDER_ORIGIN="https://tranco-list.eu";var PROVIDER_TIMEOUT_MS=6e3;var MAX_LINK_RESPONSE_BYTES=2*1024*1024;var MAX_RANK_RESPONSE_BYTES=256*1024;var CACHE_TTL_MS=24*60*60*1e3;var PARTIAL_CACHE_TTL_MS=15*60*1e3;var MAX_CACHE_ENTRIES=2e3;var RANK_PROVIDER_INTERVAL_MS=1050;var signalCache=new Map;var inFlight=new Map;var rankProviderQueue=Promise.resolve();var lastRankProviderRequestAt=0;function firstInteger(value){const match=String(value||"").match(/[\d,]+/);if(!match)return null;const parsed=Number(match[0].replace(/,/g,""));return Number.isSafeInteger(parsed)?parsed:null}function normalizedDomainText(value){return String(value||"").trim().toLowerCase().replace(/^www\./,"").replace(/\.$/,"")}function linkProviderUrl(domain){const url=new URL(LINK_PROVIDER_PATH,LINK_PROVIDER_ORIGIN);url.searchParams.set("domain",domain);url.searchParams.set("majesticMillionType","2");return url.toString()}function rankProviderUrl(domain){return new URL(`/api/ranks/domain/${encodeURIComponent(domain)}`,RANK_PROVIDER_ORIGIN).toString()}function parseMajesticMillionPage(html,requestedDomain,fetchedAt=new Date().toISOString()){const domain=normalizedDomainText(requestedDomain);const $=load(html);const pageText=$.root().text().replace(/\s+/g," ");const explicitlyAbsent=/is not in the Majestic Million/i.test(pageText);if(!pageText.includes("Majestic Million")||!explicitlyAbsent&&!pageText.includes("Referring Sub")){throw new Error("Link provider response format was not recognized.")}const datasetDate=pageText.match(/Top Million Root Domains List generated on\s+(.+?)\s+using data/i)?.[1]?.trim()||null;let matchingRow=null;$("a.domainname").each((_index,element)=>{if(matchingRow)return;if(normalizedDomainText($(element).text())===domain)matchingRow=$(element).closest("tr")});const base={domain,datasetDate,fetchedAt,source:"Majestic Million",sourceUrl:`${LINK_PROVIDER_ORIGIN}${LINK_PROVIDER_PATH}`,license:"CC BY 3.0",scope:"public_top_million"};if(!matchingRow){return{...base,found:false,globalRank:null,tldRank:null,referringSubnets:null,referringIps:null}}const cells=matchingRow.children("td").map((_index,cell)=>$(cell).text().replace(/\s+/g," ").trim()).get();if(cells.length<7)throw new Error("Link provider result row was incomplete.");return{...base,found:true,globalRank:firstInteger(cells[0]),tldRank:firstInteger(cells[4]),referringSubnets:firstInteger(cells[5]),referringIps:firstInteger(cells[6])}}function parseTrancoRankResponse(value,requestedDomain){const domain=normalizedDomainText(requestedDomain);if(!value||typeof value!=="object"||!Array.isArray(value.ranks)){throw new Error("Web rank provider response format was not recognized.")}const returnedDomain=normalizedDomainText(String(value.domain||""));if(returnedDomain&&returnedDomain!==domain)throw new Error("Web rank provider returned a different domain.");const history=value.ranks.map(item=>{if(!item||typeof item!=="object")return null;const date=String(item.date||"");const rank=Number(item.rank);if(!/^\d{4}-\d{2}-\d{2}$/.test(date)||!Number.isSafeInteger(rank)||rank<1)return null;return{date,rank}}).filter(item=>item!=null).sort((left,right)=>right.date.localeCompare(left.date)).slice(0,31);return{domain,status:history.length?"measured":"not_ranked",history}}async function boundedResponseText(response,maximumBytes){const advertisedLength=Number(response.headers.get("content-length")||0);if(advertisedLength>maximumBytes)throw new Error("Provider response was too large.");if(!response.body)return response.text();const reader=response.body.getReader();const decoder=new TextDecoder;let total=0;let text="";while(true){const{done,value}=await reader.read();if(done)break;total+=value.byteLength;if(total>maximumBytes){await reader.cancel();throw new Error("Provider response was too large.")}text+=decoder.decode(value,{stream:true})}return text+decoder.decode()}async function fetchBacklinkSnapshot(domain){const controller=new AbortController;const timeout=setTimeout(()=>controller.abort(),PROVIDER_TIMEOUT_MS);try{const response=await fetch(linkProviderUrl(domain),{signal:controller.signal,redirect:"error",headers:{Accept:"text/html,application/xhtml+xml","User-Agent":"Crawlio/1.0 (+https://keywordsintel.vercel.app; domain-signals)"}});if(!response.ok)throw new Error(`Link provider returned HTTP ${response.status}.`);if(!String(response.headers.get("content-type")||"").toLowerCase().includes("text/html")){throw new Error("Link provider returned an unexpected content type.")}return parseMajesticMillionPage(await boundedResponseText(response,MAX_LINK_RESPONSE_BYTES),domain)}finally{clearTimeout(timeout)}}function wait(milliseconds){return new Promise(resolve=>setTimeout(resolve,milliseconds))}function withRankProviderSlot(work){const result=rankProviderQueue.catch(()=>void 0).then(async()=>{const delay=Math.max(0,RANK_PROVIDER_INTERVAL_MS-(Date.now()-lastRankProviderRequestAt));if(delay)await wait(delay);lastRankProviderRequestAt=Date.now();return work()});rankProviderQueue=result.then(()=>void 0,()=>void 0);return result}async function fetchWebRankSnapshot(domain){return withRankProviderSlot(async()=>{const controller=new AbortController;const timeout=setTimeout(()=>controller.abort(),PROVIDER_TIMEOUT_MS);try{const response=await fetch(rankProviderUrl(domain),{signal:controller.signal,redirect:"error",headers:{Accept:"application/json","User-Agent":"Crawlio/1.0 (+https://keywordsintel.vercel.app; domain-signals)"}});if(!response.ok)throw new Error(`Web rank provider returned HTTP ${response.status}.`);if(!String(response.headers.get("content-type")||"").toLowerCase().includes("application/json")){throw new Error("Web rank provider returned an unexpected content type.")}return parseTrancoRankResponse(JSON.parse(await boundedResponseText(response,MAX_RANK_RESPONSE_BYTES)),domain)}finally{clearTimeout(timeout)}})}function cached(domain){const entry=signalCache.get(domain);if(!entry)return null;if(entry.expiresAt<=Date.now()){signalCache.delete(domain);return null}return entry.value}function cache(domain,value){if(signalCache.size>=MAX_CACHE_ENTRIES){const oldestKey=signalCache.keys().next().value;if(oldestKey)signalCache.delete(oldestKey)}signalCache.set(domain,{expiresAt:Date.now()+(value.partial?PARTIAL_CACHE_TTL_MS:CACHE_TTL_MS),value})}async function loadSignals(domain){const[linkResult,rankResult]=await Promise.allSettled([fetchBacklinkSnapshot(domain),fetchWebRankSnapshot(domain)]);if(linkResult.status==="rejected"&&rankResult.status==="rejected")throw new Error("Domain signal providers are unavailable.");const backlink=linkResult.status==="fulfilled"?linkResult.value:null;const rank=rankResult.status==="fulfilled"?rankResult.value:null;const history=rank?.history||[];const currentRank=history[0]?.rank??null;const previousRank=history.length>1?history[history.length-1].rank:null;const fetchedAt=backlink?.fetchedAt||new Date().toISOString();return{domain,found:Boolean(backlink?.found),globalRank:backlink?.globalRank??null,tldRank:backlink?.tldRank??null,referringSubnets:backlink?.referringSubnets??null,referringIps:backlink?.referringIps??null,datasetDate:backlink?.datasetDate??null,fetchedAt,source:"Majestic Million",sourceUrl:`${LINK_PROVIDER_ORIGIN}${LINK_PROVIDER_PATH}`,license:"CC BY 3.0",scope:"public_top_million",linkStatus:linkResult.status==="rejected"?"unavailable":backlink?.found?"measured":"not_ranked",webRankStatus:rankResult.status==="rejected"?"unavailable":rank?.status||"not_ranked",webRank:currentRank,previousWebRank:previousRank,webRankChange:currentRank!=null&&previousRank!=null?previousRank-currentRank:null,webRankHistory:history,partial:linkResult.status==="rejected"||rankResult.status==="rejected",attributions:[{label:"Majestic Million",url:`${LINK_PROVIDER_ORIGIN}${LINK_PROVIDER_PATH}`,license:"CC BY 3.0"},{label:"Tranco",url:"https://tranco-list.eu/"}]}}async function getPublicLinkSignals(domainInput){const domain=normalizedDomainText(domainInput);const existing=cached(domain);if(existing)return existing;const currentRequest=inFlight.get(domain);if(currentRequest)return currentRequest;const request=loadSignals(domain).then(value=>{cache(domain,value);return value}).finally(()=>inFlight.delete(domain));inFlight.set(domain,request);return request}export{getPublicLinkSignals,parseMajesticMillionPage,parseTrancoRankResponse};
