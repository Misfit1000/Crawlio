# Review-first rollout

The first 30 automatic articles require explicit administrator review by default. The threshold is configurable from 30 to 50. Rejected and failed work does not count as approved.

Strict Autopilot stays locked until the approval threshold is met and remains disabled by default afterward. Emergency pause, publication pause, uncertain claims, and failed quality gates always override automatic publishing.
