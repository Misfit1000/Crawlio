# Link terminology

Internal links connect Crawlio pages. External references link from Crawlio to source websites. Backlinks are genuine inbound links from other websites to Crawlio.

The blog platform creates internal links and external references. It does not create backlinks. Both rendered link types use crawlable `<a href>` elements with descriptive anchors.
