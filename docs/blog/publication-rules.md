# Publication rules

The application selects times from the configured timezone, windows or fixed time, spacing, daily limit, blackout dates, maintenance state, review requirement, and completed quality gates. Model timing recommendations are advisory only.

Every decision stores the selected time, rule, urgency, spacing decision, administrator override, and actor. Paused or conflicted articles remain unpublished.
