# Section regeneration

Editors can queue a revision for an introduction, heading section, conclusion, tagline, summary, or meta description. The worker sends only the brief, selected section, adjacent headings, and relevant sources.

The current article remains unchanged while the revision is pending. Citation, claim, originality, link, and heading checks run before a side-by-side diff is shown. Accept applies only the selected section; Reject preserves the article.
